# GMAM Schema Gen — 版本规划

| 版本 | 内容 |
|------|------|
| **v0.0.1** | 读写 `.gmam.txt`、校验、行内编辑、增删移行、导出 C++、`build_exe.py` |
| **v0.0.2**（当前） | 新建 data/header 单选；表格列对齐；`./lang` 中英文翻译（启动生成）；枚举中英对照；界面语言切换 |

数据文件内仍使用与 C++ 一致的英文枚举名（`Numeric`、`FillChar` 等）。
