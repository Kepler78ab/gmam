# GMAM Schema 生成器 — 计划清单

> 状态：✅ v0.0.2 已实现  

## v0.0.1

- [x] model + format_io + cpp_export  
- [x] Tkinter：新建/打开/保存/另存为  
- [x] 行内表格 + 增删移行  
- [x] 导出 C++  
- [x] `build_exe.py`  
- [x] samples + 使用说明  

## v0.0.2

- [x] 新建 data/header **单选**（非手输）  
- [x] 表格统一 grid **列对齐**  
- [x] `./lang` 中英文翻译文件（启动缺失则生成）  
- [x] 枚举下拉 `英文码 — 译文`；菜单语言切换  

## 审查定案

行内编辑；name 重复禁止保存；length>18 警告可保存；导出 C++；exe `-F -w`；扩展名 `.gmam.txt`；`.gmam.txt` 内英文枚举不变。
