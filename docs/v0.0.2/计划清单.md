# GMAM v0.0.2 计划清单

> 状态：✅ 已实现（请本地编译验证）  
> 范围：仅 §13 错误收集（`QStringList`）  
> 对齐/填充 → v0.0.3  

---

## 0. 范围

### 0.1 做

- [x] `build(..., QStringList& errors)`，删除 `QString&` 重载  
- [x] 整体级立即返回；字段级收集 + 失败占位  
- [x] 错误文案：字段名 / 序号 / 类型 / 原因  
- [x] 自测：多错误；回归 smoke + T9  
- [x] 文档与变更记录  

### 0.2 不做

- [ ] 对齐 / pad / missing / MissingPolicy（v0.0.3）  
- [ ] 类型枚举合并、错误码枚举  

---

## 1. 实现项

- [x] `fieldTypeName` / `formatFieldError`（`GmamError.h`）  
- [x] `encodeField` 短 reason  
- [x] `DataContentBuilder` 收集流程  
- [x] `HeaderBuilder` 收集流程  
- [x] 测试与使用说明  

---

## 3. 审查

| 项 | 结论 |
|----|------|
| 错误 API | ✅ 仅 QStringList |
| 对齐填充 | → v0.0.3 |

**本包结论**：✅ 已开工并落地  

请本地运行 `gmamRunAllTests()` 确认。
