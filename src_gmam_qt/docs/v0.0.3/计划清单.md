# GMAM v0.0.3 计划清单

> 状态：✅ 已按推荐实现（请本地编译验证）

## 定案

| 项 | 结论 |
|----|------|
| 哨兵 `'\0'` | 接受 |
| Align + pad + missing | 全做 |
| MissingPolicy | 做（默认 TreatAsMissing） |
| Truncate 跟 align | 是 |
| 失败占位用 missingFill | 是 |
| overflow | **P0** |

## 实现勾选

- [x] 类型扩展 + `resolveFieldStyle` / `shouldUseMissingFill`
- [x] FieldCodec 对齐补位与 Truncate
- [x] Builder 缺值 / 失败占位
- [x] 自测 + 使用说明 + 变更记录
